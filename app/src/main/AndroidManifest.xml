<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.felix_its.allaplicationdemo">
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.MakeCallActivity" />
        <activity android:name=".Activity.SendSMSActivity" />
        <activity android:name=".Activity.OpenBrouserActivity" />
        <activity android:name=".Activity.SendMailActivity" />
        <activity android:name=".Activity.OpenWhatsappActivity" />
        <activity android:name=".Activity.OpenFacebookActivity"></activity>
    </application>

</manifest>